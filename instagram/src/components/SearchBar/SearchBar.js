import React from 'react';

const SearchBar = () => {
    return (
        <div>
            {/* logo */}
            {/* search input */}
            {/* icons */}
        </div>
    );
};

export default SearchBar;